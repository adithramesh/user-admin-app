<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #fff; /* White background */
            color: #000; /* Black text */
            margin: 0;
            padding: 0;
        }
        .container {
            padding: 20px;
        }
        h1 {
            color: #000; /* Black heading */
        }
        .greeting {
            color: #000; /* Black text for greeting */
            margin-bottom: 20px;
        }
        .email {
            color: #000; /* Black text for email ID */
            font-weight: bold;
        }
        .logout-btn {
            background-color: #000; /* Black background for logout button */
            color: #fff; /* White text for logout button */
            border: none;
            padding: 10px 20px;
            border-radius: 3px;
            cursor: pointer;
        }
        .logout-btn:hover {
            background-color: #333; /* Darker shade on hover */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome to the Dashboard</h1>
        <div class="greeting">Hello, <span class="email"><%= userEmail %></span></div>
        <form action="/user/logout" method="POST">
            <button type="submit" class="logout-btn">Logout</button>
        </form>
    </div>
</body>
</html>